package ffi_argv;
require core.basic;

[[(:foreign_origin, :builtin), (:foreign_linkage, :static)]]
foreign "core/ffi/argv" ~> .
{
	+ glc_argv_count() -> basic.u8;
	+ glc_argv_get(index<basic.u8>) -> [basic.c1, :c];
}
